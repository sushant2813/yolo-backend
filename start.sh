#!/bin/bash

# Activate the virtual environment (if applicable)
# source /path/to/your/virtualenv/bin/activate

# Install required dependencies (you can also run this manually once)
pip install -r requirements.txt

# Run the Flask app
python3 app.py
